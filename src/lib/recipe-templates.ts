import projectToml from '../data/recipes/project.toml?raw';
import projectLock from '../data/recipes/uv.lock?raw';
import publishCi from '../data/recipes/publish-ci.py?raw';
import publishWorkflow from '../data/recipes/publish-workflow.yml?raw';
import serveAdapter from '../data/recipes/serve-adapter.py?raw';
import serveVllm from '../data/recipes/serve-vllm.py?raw';

type RecipeDocument = {
  recipe_sha256: string; outcome: string;
  target: { accelerator: string; runtime: string };
  inputs: { generator: { repository: string; revision: string; files: string[] }; dataset: unknown };
  configuration: { framework: string; ui: string; observability: boolean };
};
export function projectFiles(recipe: RecipeDocument): Record<string, string> {
  const rag = recipe.outcome === 'rag';
  const train = recipe.outcome === 'sft';
  const docker = !train && recipe.target.accelerator === 'cpu' && recipe.inputs.generator.files.some(name => name.endsWith('.safetensors')) && !['llama.cpp', 'mlx'].includes(recipe.target.runtime);
  const extras = [train ? '--extra train' : '', recipe.target.runtime === 'mlx' ? '--extra apple' : '', recipe.configuration.observability ? '--extra observe' : '', recipe.configuration.framework === 'langchain' ? '--extra langchain' : '', recipe.configuration.ui === 'gradio' ? '--extra gradio' : ''].filter(Boolean).join(' ');
  const files: Record<string, string> = {
    'superii-recipe.json': JSON.stringify(recipe, null, 2) + '\n',
    'pyproject.toml': projectToml,
    'uv.lock': projectLock,
    '.python-version': '3.12\n',
    '.gitignore': '.venv/\n.env\n__pycache__/\n.pytest_cache/\n.superii/\nindex/\ndocuments/\nadapter/\nruns/\nsecrets/\n',
    '.dockerignore': '.git\n.venv\n.env\n__pycache__\n.pytest_cache\nsecrets\ndocuments\nadapter\nruns\n.superii\n',
    '.env.example': 'SUPERII_APP_TOKEN=\n# Optional for private input repositories; never commit a token.\nSUPERII_TOKEN=\n# Optional distinct read tokens for private inputs:\nSUPERII_GENERATOR_TOKEN=\nSUPERII_EMBEDDING_TOKEN=\nSUPERII_DATASET_TOKEN=\n# Set this explicitly to enable trace export.\n# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318\n',
    'test_project.py': `from superii.recipes import Recipe\n\ndef test_immutable_contract():\n    recipe = Recipe.read()\n    assert recipe.sha256 == ${JSON.stringify(recipe.recipe_sha256)}\n    assert recipe.document["verification"]["model_quality"] == "not-established"\n`,
    'README.md': `# Super ii ${rag ? 'RAG application' : train ? 'SFT with LoRA' : 'model API'}\n\nSource: ${recipe.inputs.generator.repository}\nRevision: ${recipe.inputs.generator.revision}\nRecipe: ${recipe.recipe_sha256}\n\n## Run locally\n\nInstall Python 3.12 and uv, then review the generated files.\n\n\`\`\`sh\nuv sync --frozen ${extras}\nuv run pytest\n${train ? 'uv run python train.py' : (rag ? 'uv run python ingest.py\nuv run python evaluate.py cases.jsonl\n' : '') + "export SUPERII_APP_TOKEN=$(uv run python -c 'import secrets; print(secrets.token_urlsafe(32))')\nuv run uvicorn app:app --host 127.0.0.1 --port 8000"}\n\`\`\`\n\n${rag ? 'Put UTF-8 text, Markdown, CSV (text column), or JSONL (text, optional id) into documents/, or use the pinned dataset selected in the recipe. Maximum 1 MiB per file, 64 MiB per corpus. No remote URL ingestion. Supply cases.jsonl with one {"question":"your question","relevant_document_ids":["your document id"]} per line. Run ingestion first; index/index.json contains the actual document IDs. Evaluation measures document-level recall, precision, MRR, nDCG and warm retrieval p95 at concurrency 1, including query embedding and local FAISS communication. It excludes generation and startup. For local gates, create gates.json (for example {"recall_at_k":0.8}).\n\nCitations identify the retrieved source and hash; they do not establish semantic entailment or independent factual correctness. Answers without valid source labels abstain. Test your own task and corpus. On macOS FAISS runs in a separate local process to avoid conflicting OpenMP runtimes.\n\n' : ''}${train ? 'Dataset: JSONL with {"text":"..."} or {"prompt":"...","completion":"..."}. This template trains full text including the prompt. At least 10 distinct examples; exact duplicates are removed before a deterministic 90/10 train/held-out split. It reports held-out language-model loss before and after training. That is not a task-quality benchmark. Output: adapter/, including safetensors, tokenizer and immutable base/dataset lineage. CPU LoRA is verified using a tiny fixture; larger bases must pass a separate training memory estimate. QLoRA and vLLM remain unverified on NVIDIA hardware.\n\nTo return the adapter, open an existing destination repository workspace, make a new editable revision, declare its license and your rights, then run publish.py with that repository UUID and a scoped token. Uploads remain subject to central security and publication policy. Local metric gates do not bypass policy. Review the destination before setting --submit.\n\n' : 'All API routes except /health require Authorization: Bearer $SUPERII_APP_TOKEN. /ready returns 503 until models load; /metrics requires the same token. POST /v1/chat/completions accepts bounded text messages (one user question for RAG, or a model-native chat template for API projects), without streaming. POST /v1/predict accepts {"text":"your input"} and permits one in-flight inference. ' + (rag ? 'POST /v1/embed accepts {"texts":["text"]}. ' : '') + 'Browser origins are rejected. Keep the default loopback binding; use a TLS/auth reverse proxy for deployment. GGUF needs an installed llama-server with offline and key-file support. MLX needs the apple extra.\n\n'}## Evidence and reproducibility\n\nImmutable inputs, file hashes, selected settings and exact core dependency pins are recorded in superii-recipe.json. Run records under runs/ bind that recipe to actual environment, metrics and output hashes. They are reported evidence, not independent verification. The fixture checks establish execution only, not quality for this model or your data. The included uv.lock fixes transitive versions and distribution hashes. Use uv sync --frozen and keep the lock and recipe with each result. GPU exports have their own environment and remain unverified. Never put credentials, private documents, or indexes into a public repository.\n\n${docker ? '## Container\n\nThe Docker export uses CPU Transformers. Use a safetensors CPU recipe; llama.cpp and MLX require separate native runtime installations. Ingest locally first for RAG, then run:\n\n```sh\ndocker build -t superii-project .\ndocker run --rm -p 127.0.0.1:8000:8000 -e SUPERII_APP_TOKEN -e SUPERII_TOKEN superii-project\n```\n\nIt runs as an unprivileged user. /health is liveness; authenticated /ready establishes loaded model readiness.\n\n' : ''}${recipe.configuration.observability ? '## Observability\n\n/metrics exposes request counts and duration without prompts or repository credentials. Trace export is opt-in via OTEL_EXPORTER_OTLP_ENDPOINT. The generated Prometheus configuration reads secrets/app-token; create that file from your application token with mode 0600. Supply Prometheus to your existing Grafana deployment and import grafana-dashboard.json. Trace spans contain operation names only, with exception recording disabled.\n\n' : ''}## GitHub publication\n\nTraining exports include a manual GitHub workflow that uses short-lived OIDC identity. First configure the destination repository trusted publisher for your exact GitHub repository, workflow and branch. See https://superii.site/docs#trusted-publishing. GitHub-to-PyPI is a separate publisher. No deployment or publication runs automatically when this archive is downloaded.\n`,
  };
  if (!train) {
    files['app.py'] = `import os\nfrom superii.recipes import Recipe\nfrom superii.recipes.project import Project, create_app\n\napp = create_app(Project(Recipe.read()), token=os.environ.get("SUPERII_APP_TOKEN", ""))\n`;
  }
  if (docker) {
    files['Dockerfile'] = `FROM python:3.12-slim\nRUN pip install --no-cache-dir uv==0.11.24\nRUN useradd --create-home --uid 10001 runner\nWORKDIR /app\nCOPY pyproject.toml uv.lock ./\nRUN uv sync --frozen ${extras}\nCOPY --chown=runner:runner . .\nRUN chown -R runner:runner /app\nUSER runner\nENV PATH="/app/.venv/bin:$PATH"\nEXPOSE 8000\nHEALTHCHECK --interval=30s --timeout=5s CMD python -c "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health')"\nCMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]\n`;
  }
  if (rag) {
    files['ingest.py'] = `from superii.recipes import Recipe\nfrom superii.recipes.project import Project\n\nif __name__ == "__main__":\n    project = Project(Recipe.read())\n    try:\n        print(project.ingest())\n    finally:\n        project.close()\n`;
    files['evaluate.py'] = `import json\nimport sys\nfrom pathlib import Path\nfrom superii.recipes import Recipe\nfrom superii.recipes.project import Project\nfrom superii.recipes.evaluation import evaluate_rag\n\nif __name__ == "__main__":\n    project = Project(Recipe.read())\n    try:\n        gates = json.loads(Path("gates.json").read_text()) if Path("gates.json").exists() else {}\n        metrics, record = evaluate_rag(project.load_index(), Path(sys.argv[1]), gates=gates)\n        print(json.dumps(metrics, indent=2))\n        print(record)\n    finally:\n        project.close()\n`;
  }
  if (train) {
    files['serve_adapter.py'] = serveAdapter;
    files['publish_ci.py'] = publishCi;
    files['.github/workflows/publish-project.yml'] = publishWorkflow;
    files['README.md'] += '\n## Run the adapter\n\nSet SUPERII_APP_TOKEN and run uv run python serve_adapter.py runs/YOUR-RUN/superii-run.json. The loader verifies every recorded adapter file before attaching it to the exact acquired base model.\n\nFor GitHub, configure SUPERII_DESTINATION_ID as a repository variable, an editable destination revision and its rights declaration. Configure the trusted publisher for this exact GitHub repository, main branch, and .github/workflows/publish-project.yml with read, upload, commit and submit scopes. Dispatch Train and submit Super ii adapter manually. Source inputs must be accessible from the runner; use separate input credentials for private sources. The job trains on its CPU, and requests publication credentials only after training.\n';
    files['train.py'] = `from superii import Client\nfrom superii.recipes import Recipe\nfrom superii.recipes.training import train\n\nif __name__ == "__main__":\n    with Client() as client:\n        print(train(Recipe.read(), client))\n`;
    files['publish.py'] = `from superii.recipes.publication import main\n\nif __name__ == "__main__":\n    main()\n`;
  }
  if (recipe.configuration.observability && !train) {
    files['prometheus.yml'] = `global:\n  scrape_interval: 15s\nscrape_configs:\n  - job_name: superii-project\n    authorization:\n      credentials_file: secrets/app-token\n    static_configs:\n      - targets: ['localhost:8000']\n`;
    files['grafana-dashboard.json'] = JSON.stringify({ title: 'Super ii project', schemaVersion: 41, tags: ['superii'], panels: [
      { id: 1, title: 'Requests per second', type: 'timeseries', gridPos: { x: 0, y: 0, w: 12, h: 8 }, targets: [{ expr: 'sum(rate(superii_project_requests_total[5m]))' }] },
      { id: 2, title: 'Request p95 seconds', type: 'timeseries', gridPos: { x: 12, y: 0, w: 12, h: 8 }, targets: [{ expr: 'histogram_quantile(0.95, sum by(le) (rate(superii_project_request_seconds_bucket[5m])))' }] },
    ] }, null, 2) + '\n';
  }
  if (recipe.configuration.framework === 'langchain' && !train) {
    files['chain.py'] = `from langchain_core.runnables import RunnableLambda\n\ndef make_chain(project):\n    """Pass an already loaded project; no remote model or tracing provider is added."""\n    return RunnableLambda(project.predict)\n`;
    files['README.md'] += '\n## LangChain\n\nInstall the langchain extra. Import make_chain from chain.py and pass a loaded Project; chain.invoke(text) returns the same source-aware result as the Python API. Disable external LangChain/LangSmith tracing unless you intend to send input data there.\n';
  }
  if (recipe.configuration.ui === 'gradio' && !train) {
    files['ui.py'] = `import os\nimport json\nimport gradio as gr\nfrom superii.recipes import Recipe\nfrom superii.recipes.project import Project\n\ndef make_ui(project):\n    return gr.Interface(fn=lambda text: json.dumps(project.predict(text), ensure_ascii=False, indent=2), inputs=gr.Textbox(label="Question or prompt"), outputs=gr.Textbox(label="Result and sources"), title="Super ii project", analytics_enabled=False)\n\nif __name__ == "__main__":\n    token = os.environ.get("SUPERII_APP_TOKEN", "")\n    if len(token) < 32:\n        raise ValueError("Set SUPERII_APP_TOKEN to a random value of 32+ characters")\n    project = Project(Recipe.read())\n    try:\n        project.load()\n        make_ui(project).queue(default_concurrency_limit=1, max_size=8).launch(server_name="127.0.0.1", share=False, auth=("superii", token), show_error=False, blocked_paths=[os.path.abspath("documents"), os.path.abspath("index"), os.path.abspath(".env"), os.path.expanduser("~/.cache/superii")])\n    finally:\n        project.close()\n`;
    files['README.md'] += '\n## Gradio\n\nRun uv run python ui.py after ingestion (for RAG) and set SUPERII_APP_TOKEN as above. Sign in locally as superii using that token. Sharing is disabled; inputs and outputs stay in this process. This export does not establish compatibility with the Super ii protected Apps host.\n';
  }
  files['gpu/README.md'] = '# NVIDIA exports: unverified\n\nNo NVIDIA host was available for execution tests. These exports require your own CUDA host and a separate environment; they are not a hosted service or a measured performance claim. The default project remains on the verified CPU/Apple path. Review the vendor hardware requirements and run your own workload evaluation before deployment.\n\nInstall the requirements from this folder into a separate virtual environment. Run the script from the project root and explicitly pass --enable-unverified-gpu. It still checks actual hardware and memory and verifies immutable input files. No automatic weight conversion.\n';
  if (train) {
    files['gpu/requirements.txt'] = 'superii-sdk[train]==0.2.0\nbitsandbytes==0.50.2\ntorch==2.14.0\ntransformers==5.16.1\npeft==0.20.0\ntrl==1.12.0\naccelerate==1.14.0\ndatasets==5.0.1\n';
    files['gpu/train_qlora.py'] = `import argparse\nfrom pathlib import Path\nfrom superii import Client\nfrom superii.recipes import Recipe\nfrom superii.recipes.contracts import canonical, digest\nfrom superii.recipes.training import train\n\nif __name__ == "__main__":\n    parser = argparse.ArgumentParser(description="Unverified NVIDIA QLoRA export")\n    parser.add_argument("--enable-unverified-gpu", action="store_true", required=True)\n    parser.parse_args()\n    value = Recipe.read().document\n    value["target"] = {"accelerator": "cuda", "python": "3.12", "runtime": "transformers"}\n    value["configuration"]["adapter"] = "qlora"\n    value["dependencies"]["bitsandbytes"] = "0.50.2"\n    value["verification"]["hardware"] = []\n    value["recipe_sha256"] = digest({k: v for k, v in value.items() if k != "recipe_sha256"})\n    gpu = Recipe(value)\n    record = Path("superii-qlora-recipe.json")\n    if record.exists():\n        raise ValueError("Use a new project directory for a new GPU experiment")\n    record.write_bytes(canonical(value) + b"\\n")\n    with Client() as client:\n        print(train(gpu, client, output=Path("gpu-adapter")))\n`;
  } else {
    files['gpu/requirements.txt'] = 'superii-sdk[serve]==0.2.0\nvllm==0.28.0\ntorch==2.13.0\n';
    files['gpu/serve_vllm.py'] = serveVllm;
  }
  return files;
}
