---
import RepositoryPage from '@/components/RepositoryPage.astro';

const owner = Astro.params.owner ?? '';
const slug = Astro.params.slug ?? '';
---
<RepositoryPage kind="space" owner={owner} slug={slug} />
